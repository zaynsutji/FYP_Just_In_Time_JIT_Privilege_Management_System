# -Just-In-Time-JIT-Privilege-Management-System
system uses an internal Certificate Authority (CA) to issue short-lived SSH certificates that auto-expire within minutes, eliminating standing privileges and securing remote server access.
